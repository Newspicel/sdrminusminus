# macOS
.DS_Store

# Rust
/target
**/target
# Cargo.lock IS committed: this workspace ships binaries and needs reproducible builds.

# Python
__pycache__/
*.pyc

# Node / web
node_modules
dist
.pnpm-store

# Tauri
apps/desktop/gen

# Generated (checked in, but ignore local build noise)
web/src/generated/*.tsbuildinfo

# Editor
.idea
.vscode
*.swp

# Env
.env
.env.local

# mdBook build output (docs/ is the source; the site is built in CI).
docs/book

# Off-air IQ captures from hardware sessions: hundreds of MB each, kept locally as
# reproductions for decoder work (fixtures/ holds the small synthesized pairs instead).
captures
web/.e2e-tmp/
web/.shots-tmp/
web/test-results/
web/playwright-report/
