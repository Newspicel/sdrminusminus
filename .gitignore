# macOS
.DS_Store

# Rust
/target
**/target
# Cargo.lock IS committed: this workspace ships binaries (reproducible builds, PLAN §15).

# Node / web
node_modules
dist
.pnpm-store

# Tauri
apps/desktop/gen

# Generated (checked in, but ignore local build noise)
web/src/generated/*.tsbuildinfo

# Editor
.idea
.vscode
*.swp

# Env
.env
.env.local

# mdBook build output (docs/ is the source; the site is built in CI).
docs/book
