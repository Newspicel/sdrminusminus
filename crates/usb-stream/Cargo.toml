[package]
name = "sdrmm-usb-stream"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[features]
# Scripted endpoints for the backends above this crate, so the burst semantics a radio layers
# on the shared queue are tested against the same mock this crate's own tests use.
test-util = []

[dependencies]
nusb.workspace = true
thiserror.workspace = true
tracing.workspace = true
